# Telegram API credentials
API_ID=your_api_id
API_HASH=your_api_hash
BOT_TOKEN=your_bot_token

# Security salt for user ID hashing (required)
SALT=your_secure_random_salt_string

# Banned words (comma-separated, supports Finnish and English)
BANNED_WORDS=spam,scam,virus,hack,huijaus,petos,kiusaaminen,kirosana,vittu,perkele

# Forward group IDs (optional, comma-separated list of chat IDs)
# If set, skips auto-discovery and uses these specific groups/channels for message forwarding
# Supports topic syntax: chat_id:topic_id (topic IDs are stored but not used for routing yet)
# Example: FORWARD_GROUP_IDS=-1001234567890,-1009876543210:55,-1002334445566
# Use /id command in target chats to get their IDs
# FORWARD_GROUP_IDS=

# Username enforcement (default: enabled)
# ENFORCE_USERNAME=1  # 1=kick users without username, 0=allow all users

# Optional message sent before kicking users without username
# USERNAME_KICK_NOTICE=Please set a public @username to participate in this group.

# Forward delay configuration (seconds)
# Random delay applied per target when forwarding messages
# FORWARD_DELAY_MIN=0.8   # Lower bound for random delay
# FORWARD_DELAY_MAX=2.4   # Upper bound for random delay